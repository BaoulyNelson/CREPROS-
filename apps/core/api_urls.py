# """Routes API REST (extensible — base pour de futures intégrations : app mobile, etc.)."""
# from rest_framework.routers import DefaultRouter

# from apps.actualites.api import ArticleViewSet
# from apps.evenements.api import EvenementViewSet
# from apps.recherches.api import RechercheViewSet

# routeur = DefaultRouter()
# routeur.register('recherches', RechercheViewSet, basename='api-recherches')
# routeur.register('articles', ArticleViewSet, basename='api-articles')
# routeur.register('evenements', EvenementViewSet, basename='api-evenements')

# urlpatterns = routeur.urls
